# Input Format
"""1.The first line contains a single integer T - the number of test cases. Then the test cases follow.
2.The first and only line of each test case contains two integers 
X and H - the height of Chef's son and the minimum height required for the ride respectively."""
# Output Format
"""For each test case, output in a single line, YES if Chef's son can go on the ride. Otherwise, output NO.
You may print each character of YES and NO in uppercase or lowercase (for example, yes, yEs, Yes will be considered identical)"""
# Constraints --- 1≤T≤1000 & 1≤X,H≤100

# Code

n = int(input())
for i in range(n):
    X, h = map(int,input().split())
    
    if X>=h:
        print("YES")
    else:
        print("NO")

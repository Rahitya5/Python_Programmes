# Input Format
"""1.The first line of input will contain an integer 
T — the number of test cases. The description of T test cases follows.
2.The first and only line of each test case contains two space-separated integers 
A and B, the number of patties and buns respectively."""
# Output Format
# For each test case, output the maximum number of burgers that Chef can make.
# Constraints ----- 1≤T≤1000 & 1≤A,B≤10^5

# code

t=int(input())
for i in range(t):
    a,b=map(int,input(). split (' '))
    if a<=b:
        print(a)
    else: 
        print(b)
